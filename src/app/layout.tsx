import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { ThemeProvider } from "./provider";
import Navigation from "./components/Navigation";
import { SidebarProvider, SidebarTrigger } from "@/components/ui/sidebar";
import { AppSidebar } from "@/components/app-sidebar";
import Footer from "./components/Footer";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
	title: "Walkthrough Guide",
	authors: [{ name: "Torres J. Dev" }],
	description: "Generated by create next app",
	icons:
		"https://torresjdev.github.io/Nextjs-Asset-Host/assets/icons/dev/dev-md.svg",
};

export default function RootLayout({
	children,
}: Readonly<{
	children: React.ReactNode;
}>) {
	return (
		<html lang="en">
			<body className={`gradient-background ${inter.className}`}>
				<div className="gradient-background"></div>
				<Navigation />
				<ThemeProvider
					attribute="class"
					defaultTheme="dark"
					enableSystem
					disableTransitionOnChange
				>
					<SidebarProvider>
						<AppSidebar />
						<main className="min-h-full ml-[10px] relative flex top-14">
							<SidebarTrigger />
							{children}
						</main>
					</SidebarProvider>
				</ThemeProvider>
				<Footer />
			</body>
		</html>
	);
}
